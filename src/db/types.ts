// Kysely's generated schema shape. Empty until Phase 1's migrations land;
// filled in table-by-table as each migration is written, not generated
// speculatively ahead of the schema that doesn't exist yet.
export interface Database {}
